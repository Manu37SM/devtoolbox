{
  "name": "@devtoolbox/cli",
  "version": "0.1.0",
  "private": true,
  "description": "Command-line client for the DevToolbox Public API (API.md §12).",
  "bin": {
    "devtoolbox": "dist/cli/src/index.js"
  },
  "main": "dist/cli/src/index.js",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "eslint .",
    "typecheck": "tsc --noEmit",
    "test": "node --import tsx --test src/**/*.test.ts"
  },
  "dependencies": {
    "@devtoolbox/shared": "*"
  },
  "devDependencies": {
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
